#include <iostream>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	int a, b, c, d, ch, zn;
	char l;
	cout << "Введите первую дробь: ";
	cin >> a >> l >> b;
	cout << "Введите вторую дробь: ";
	cin >> c >> l >> d;
	ch = (a*d) + (b* c);
	zn = b * d;
	cout << "\n\nСумма равна " << ch << "/" << zn;
	return 0;
}
